backend:
  name: git-gateway
  branch: master
publish_mode: editorial_workflow
media_folder: src/images
public_folder: /images
collections:
  - name: pages
    label: Pages
    folder: site/content
    create: false
    slug: {{slug}}
    fields:
      - label: Title
        name: title
        widget: string
    